#include <iostream>
#include <iterator>

#include "max_subarray.hpp"

namespace array {
namespace maxsub {

std::vector<int> solve(std::vector<int>& seq) {
  std::vector<int> subarray{};

  return std::move(subarray);
}

}  // namespace maxsub
}  // namespace array
